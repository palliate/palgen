#pragma once
#include <cstdint>

namespace config {
namespace resource {
  $%- for resource in resources %$
  static constexpr uint8_t ${ resource.name }$[] = {
      ${ resource.data }$
  };
  $% endfor %$
} // namespace resource
} // namespace config
