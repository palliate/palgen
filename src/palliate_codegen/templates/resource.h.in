#pragma once
#include <cstdint>

namespace config {
namespace resource {
  $%- for resource in resources %$
  static constexpr $% if resource.text -%$
    char
  $%- else -%$
    uint8_t
  $%- endif %$ ${ resource.name }$[] = {
      ${ resource.data }$
  };
  $% endfor %$
} // namespace resource
} // namespace config
